# 分片上传、秒传、断点续传与 HLS 播放

## 启用

使用项目要求的 pnpm 11（当前锁文件为 pnpm 11.24.0 格式）安装依赖，然后在启动后端前执行：

```sh
pnpm install
pnpm --dir apps/server run db:generate
pnpm --dir apps/server run db:migrate
```

新增迁移 `20260910190000_multipart_upload_hls` 添加分片记录、合并租约和 HLS 产物信息，不删除原文件。不要在未迁移的数据库上直接启动新版上传模块。

后端需要 PostgreSQL、Redis、RabbitMQ，以及包含 `libx264`、AAC、`libwebp` 和 HLS muxer 的 FFmpeg/ffprobe。继续使用 `FFMPEG_PATH`、`FFPROBE_PATH` 指定可执行文件路径，不包含命令行参数。

后端在 WSL 内运行时，直接使用 WSL 的 `ffmpeg`、`ffprobe`。Windows 后端应使用 Windows 可执行文件；跨 Windows/WSL 启动程序还需要转换输入和输出路径，不能将 `FFMPEG_PATH` 直接设置为 `wsl ffmpeg`。

## 上传行为

- 文件大小 **大于 5 MiB（5 × 1024 × 1024 字节）** 时必须分片；等于或小于此值仍走原始二进制 PUT。
- 每片 5 MiB，最后一片按剩余大小发送。前端每文件最多并发 3 片、最多处理 2 个文件。
- Web Worker 增量计算整个文件和每个分片的 BLAKE3，不一次性把大文件读入主线程内存。
- 分片状态保存在 PostgreSQL，分片数据保存在 `STORAGE_ROOT/uploads`，需要把数据库及存储目录持久化。
- 可暂停、继续；断网后重试只补传缺失部分。刷新页面或服务重启后，**重新选择同名、同内容文件**即可恢复。浏览器无法在刷新后未经用户选择重新读取本地文件。
- 分片会话自创建起有效 24 小时，小文件会话保持原来的 30 分钟。取消、过期、失败和完成后的分片由服务端清理，定时清理间隔为 5 分钟。
- 秒传仅复用**当前用户自己已成功上传且原文件仍存在**的相同 BLAKE3、相同大小文件，返回已有资产，不复制或重命名已有资产。不会暴露其他用户是否拥有某个文件。
- 仍遵循原来的图片 10 MiB、视频 512 MiB / 4 小时及内容安全限制。

### HTTP 协议

所有上传接口都沿用 Bearer 登录认证、`upload:create` 权限和现有 CSRF 保护。

| 操作                 | 接口                                         | 说明                                                                                           |
| -------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| 创建、续传或秒传检查 | `POST /api/uploads/sessions`                 | JSON：`fileName`、`size`、`hash`；大文件必须提供 64 位小写 BLAKE3                              |
| 查询状态             | `GET /api/uploads/sessions/:id`              | 返回 `mode`、`chunkSize`、`chunkCount`、`uploadedParts`、`uploadedBytes`、`merging`、`expired` |
| 小文件上传           | `PUT /api/uploads/sessions/:id/content`      | `application/octet-stream`，超过阈值会拒绝                                                     |
| 上传分片             | `PUT /api/uploads/sessions/:id/parts/:index` | 从 0 开始；原始二进制体；必须携带 `X-Chunk-Hash`                                               |
| 合并                 | `POST /api/uploads/sessions/:id/complete`    | 顺序流式合并、校验整个文件并检查媒体内容，成功后才创建资产                                     |
| 取消                 | `DELETE /api/uploads/sessions/:id`           | 停止接受分片并清理已保存部分；已经完成的资产需在媒体库删除                                     |

同序号、同内容的重复分片是幂等的，不同内容返回 409。分片或整文件摘要不符返回 422，缺片返回 409。合并采用数据库令牌和续期租约：重复完成请求不会生成多个资产，进程中断后租约到期可恢复。临时合并失败保留分片，内容校验失败则结束该会话。

## HLS 与流式下载

后台保留原文件，生成封面和 H.264/AAC MP4，再复用已编码内容生成带独立关键帧的 MPEG-TS 分段与 VOD `index.m3u8`，目标每段约 4 秒。产物完整写入后才发布为可播放状态；历史视频首次打开 HLS 时按需补建。

视频查看器默认 HLS：支持原生 HLS 的浏览器使用原生播放，其他支持 MSE 的浏览器按需加载 `hls.js`。仅缓冲有限时长的片段，不先获取整个视频 Blob；拖动进度条只加载目标位置附近的片段。仍可切换读取原视频。

首次上传后的后台转码需要时间。这里的快速起播是指 **HLS 生成完成后无需下载整个文件即可开始播放**，不是上传未完成即可播放，也不承诺在任何网络环境下固定一秒起播。

| 操作           | 接口                                                      | 说明                                                                        |
| -------------- | --------------------------------------------------------- | --------------------------------------------------------------------------- |
| 获取流访问凭证 | `POST /api/assets/:id/stream-ticket`                      | JSON：`kind` 为 `hls`、`original` 或 `download`；需要 `asset:download` 权限 |
| 播放列表       | `GET /api/assets/:id/stream/index.m3u8?ticket=...`        | 尚未生成时，申请凭证返回 `MEDIA_PENDING`，前端显示等待并重试                |
| 视频分段       | `GET /api/assets/:id/stream/segment-000000.ts?ticket=...` | 序号有界，不能借此访问其他文件或磁盘路径                                    |
| 原文件         | `GET /api/assets/:id/stream/file?ticket=...`              | 原始内容，支持 `Range` / 206、416、HEAD 与 ETag                             |

凭证绑定资产、用户、用途和登录会话，最长有效 4 小时 15 分钟；每次请求仍检查资源归属、删除状态、当前权限及会话撤销。不要公开分享凭证链接。没有会话标识的旧登录令牌所签发的链接不会超过其 access token 有效期。

下载由浏览器原生下载器接管，不在 JavaScript 中缓存整个文件；在凭证有效期内可使用 Range 续读。关闭查看器会销毁 HLS 实例并停止未完成请求；预期的客户端提前断连不会再以 `StreamableFile` ERROR 刷屏，真正的读取错误仍记录。

部署反向代理时需允许至少 5 MiB 的单次上传体、透传 `Range` / `If-Range` / `Content-Range`，不要把文件响应整段缓冲后再发送。跨域部署要配置正确的 `CORS_ORIGIN`。

删除回收站资产时同时清理 MP4、播放列表及其所有 HLS 分段。存储容量应考虑原文件、上传暂存分片、兼容 MP4 和 HLS 的额外占用。

## 验收建议

1. 分别上传小于、等于、大于 5 MiB 的有效媒体，确认只有后者使用 `/parts/:index`。
2. 上传中暂停、断网或刷新，重新选择同一文件，确认已成功上传的分片不重传。
3. 再次选择已经完成的同一文件，确认返回原资产并显示秒传，不重复发送文件体。
4. 篡改分片摘要、漏传分片、重复调用 complete，确认分别被拒绝或幂等返回。
5. HLS 就绪后在 Network 中确认加载 `m3u8` 和少量 `.ts`，拖动播放位置不下载整个 MP4。
6. 不带凭证、篡改凭证、访问其他资产、注销后复用凭证，都应被拒绝。
7. 下载原文件并检查 Range 返回的 `206`、`Content-Range` 与实际字节内容。
